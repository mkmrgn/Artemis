# .github/workflows/retag.yml
name: Float v1 tag

on:
  push:
    branches: [ main ]

jobs:
  retag:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Move v1 tag to latest commit
        run: |
          git tag -f v1
          git push -f origin v1
